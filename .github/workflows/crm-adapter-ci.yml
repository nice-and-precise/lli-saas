name: crm-adapter-ci
# CI touch: keep workflow-triggered checks attached to integration commits.

on:
  push:
    branches: [main]
    paths:
      - "services/crm-adapter/**"
      - ".github/workflows/crm-adapter-ci.yml"
  pull_request:
    paths:
      - "services/crm-adapter/**"
      - ".github/workflows/crm-adapter-ci.yml"

jobs:
  test-build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: services/crm-adapter/package-lock.json
      - name: Install dependencies
        working-directory: services/crm-adapter
        run: npm ci
      - name: Run tests
        working-directory: services/crm-adapter
        run: npm test
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/nice-and-precise/lli-saas-crm-adapter
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
      - name: Log in to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/crm-adapter/Dockerfile
          push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

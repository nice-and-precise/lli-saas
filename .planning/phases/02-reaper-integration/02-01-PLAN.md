---
phase: 02-reaper-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/contracts
  - services/lead-engine/src
  - services/lead-engine/tests
  - services/crm-adapter/src
  - services/crm-adapter/tests
autonomous: true
requirements:
  - EXP-02
user_setup: []
must_haves:
  truths:
    - A documented internal lead contract exists and can be implemented consistently across services.
    - CRM adapter state for Monday OAuth and board selection survives process restarts.
  artifacts:
    - shared/contracts/internal-lead.schema.json
    - services/lead-engine/src
    - services/crm-adapter/src/tokenStore.js
  key_links:
    - Phase 2 plans use one contract definition before scan execution and Monday delivery deepen.
---

<objective>
Define the internal lead contract and minimal persistence primitives for Phase 2.

Purpose: Establish the schema and durable state that both the Reaper scan path and Monday delivery flow depend on.
Output: Shared lead contract artifact, lead-engine serialization helpers, and file-backed CRM persistence for OAuth token plus selected board.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reaper-integration/02-CONTEXT.md
@.planning/phases/02-reaper-integration/02-RESEARCH.md
@.planning/phases/02-reaper-integration/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the internal lead contract</name>
  <files>shared/contracts, services/lead-engine/src, services/lead-engine/tests, services/crm-adapter/src, services/crm-adapter/tests</files>
  <action>Create a repo-level contract artifact, matching lead-engine models/helpers, and crm-adapter validation/mapping helpers so both services reference the same payload shape before later feature work.</action>
  <verify>cd services/lead-engine && poetry run pytest && cd /Users/jordan/Desktop/LLI_v1/services/crm-adapter && npm test</verify>
  <done>The internal lead contract is documented and referenced by both services through automated tests or validation helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add minimal durable Monday integration state</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests</files>
  <action>Replace the in-memory token store with a file-backed persistence module that stores OAuth token and selected board data behind a stable interface.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>CRM adapter state survives restarts and remains covered by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/lead-engine && poetry run pytest`
- [ ] `cd services/crm-adapter && npm test`
- [ ] Shared contract artifact exists and is referenced by both services
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The phase has a stable lead payload definition and durable Monday state foundation
  </success_criteria>

<output>
After completion, create `.planning/phases/02-reaper-integration/02-01-SUMMARY.md`
</output>

---
phase: 02-reaper-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - services/lead-engine/src
  - services/lead-engine/tests
  - services/lead-engine/Dockerfile
autonomous: true
requirements:
  - EXP-02
user_setup: []
must_haves:
  truths:
    - Lead-engine exposes a real scan execution path instead of only a health stub.
    - Scan responses return normalized internal lead contract payloads with usable status reporting.
  artifacts:
    - services/lead-engine/src/app.py
    - services/lead-engine/src
    - services/lead-engine/tests
  key_links:
    - `/run-scan` uses the contract defined in 02-01 and does not leak raw Reaper payloads directly.
---

<objective>
Implement the real scan execution path in lead-engine.

Purpose: Turn the lead-engine scaffold into a service that can invoke the Reaper integration boundary and emit normalized lead payloads.
Output: `POST /run-scan`, Reaper adapter/orchestration code, and tests for success, validation failure, and runtime failure cases.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reaper-integration/02-CONTEXT.md
@.planning/phases/02-reaper-integration/02-RESEARCH.md
@.planning/phases/02-reaper-integration/02-VALIDATION.md
@.planning/phases/02-reaper-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Reaper integration boundary and request models</name>
  <files>services/lead-engine/src, services/lead-engine/tests</files>
  <action>Introduce the scan request/response models, add a dedicated Reaper adapter boundary, and keep route logic thin.</action>
  <verify>cd services/lead-engine && poetry run pytest</verify>
  <done>Lead-engine has a testable Reaper orchestration boundary and validated request models.</done>
</task>

<task type="auto">
  <name>Task 2: Ship POST /run-scan with normalized output</name>
  <files>services/lead-engine/src/app.py, services/lead-engine/tests, services/lead-engine/Dockerfile</files>
  <action>Implement `POST /run-scan`, normalize results to the internal lead contract, and add coverage for happy-path and error-path behavior.</action>
  <verify>cd services/lead-engine && poetry run pytest && docker build -t lli-saas/lead-engine:phase2 .</verify>
  <done>The lead-engine can execute a real scan flow and still package successfully in Docker.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/lead-engine && poetry run pytest`
- [ ] `cd services/lead-engine && docker build -t lli-saas/lead-engine:phase2 .`
- [ ] `/run-scan` returns normalized contract data and structured error responses
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Lead-engine exposes a real scan path aligned to the internal lead contract
  </success_criteria>

<output>
After completion, create `.planning/phases/02-reaper-integration/02-02-SUMMARY.md`
</output>

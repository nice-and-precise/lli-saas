---
phase: 02-reaper-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - services/crm-adapter/src
  - services/crm-adapter/tests
  - services/crm-adapter/README.md
  - services/crm-adapter/Dockerfile
autonomous: true
requirements:
  - EXP-02
user_setup: []
must_haves:
  truths:
    - CRM adapter can discover Monday boards using persisted OAuth state.
    - CRM adapter can create a Monday item from the internal lead contract using the selected board.
  artifacts:
    - services/crm-adapter/src/app.js
    - services/crm-adapter/src/mondayClient.js
    - services/crm-adapter/src/queries.js
    - services/crm-adapter/src/tokenStore.js
    - services/crm-adapter/tests
  key_links:
    - Monday board discovery and item creation consume the Phase 2 lead contract without taking on Phase 3 duplicate handling scope.
---

<objective>
Wire real Monday board discovery and lead item creation in crm-adapter.

Purpose: Make the Monday adapter capable of using the persisted OAuth state and selected board to receive normalized leads from the Phase 2 contract.
Output: Real board discovery and selection routes, item creation path, and tests proving persisted OAuth plus board selection behavior.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reaper-integration/02-CONTEXT.md
@.planning/phases/02-reaper-integration/02-RESEARCH.md
@.planning/phases/02-reaper-integration/02-VALIDATION.md
@.planning/phases/02-reaper-integration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add board discovery and selection flow</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests</files>
  <action>Use the persisted OAuth token to list boards, allow selection of one board, and store the selected board metadata durably.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>CRM adapter can discover boards and persist the board chosen for delivery.</done>
</task>

<task type="auto">
  <name>Task 2: Add lead item creation path</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests, services/crm-adapter/README.md, services/crm-adapter/Dockerfile</files>
  <action>Implement a route/service that validates and maps the shared internal lead contract at the adapter boundary, creates a Monday item on the selected board, and documents the flow for local verification.</action>
  <verify>cd services/crm-adapter && npm test && docker build -t lli-saas/crm-adapter:phase2 .</verify>
  <done>CRM adapter can create a real Monday item from a normalized lead payload and still build in Docker.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/crm-adapter && npm test`
- [ ] `cd services/crm-adapter && docker build -t lli-saas/crm-adapter:phase2 .`
- [ ] Board discovery, selection persistence, contract validation, and item creation are covered by automated tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CRM adapter can discover a board and create a lead item using persisted Monday state
  </success_criteria>

<output>
After completion, create `.planning/phases/02-reaper-integration/02-03-SUMMARY.md`
</output>

---
phase: 03-monday-delivery-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/crm-adapter/src
  - services/crm-adapter/tests
  - shared/contracts
autonomous: true
requirements:
  - EXP-01
  - EXP-03
user_setup: []
must_haves:
  truths:
    - Tenant-aware persistent delivery state exists for scans, leads, OAuth, and board mapping.
    - Monday board mapping configuration is explicit and persisted instead of being implicit in code.
  artifacts:
    - services/crm-adapter/src/tokenStore.js
    - services/crm-adapter/src/app.js
    - services/crm-adapter/tests
  key_links:
    - Phase 3 delivery logic and portal orchestration can rely on one persisted source of truth for board selection, mapping, and delivery state.
---

<objective>
Establish tenant-aware delivery persistence and board mapping foundations for Phase 3.

Purpose: Extend the existing crm-adapter persistence model into a pilot-grade tenant-aware delivery store and add board mapping APIs that later delivery and portal flows can use.
Output: Expanded persistent state shape, mapping read/write endpoints, and tests proving durable board mapping and delivery configuration behavior.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monday-delivery-flow/03-CONTEXT.md
@.planning/phases/03-monday-delivery-flow/03-RESEARCH.md
@.planning/phases/03-monday-delivery-flow/03-VALIDATION.md
@.planning/phases/02-reaper-integration/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand tenant-aware delivery state</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests</files>
  <action>Extend the current file-backed persistence so it can store tenant-scoped scan runs, delivery records, OAuth state, selected board metadata, and delivery configuration without replacing the existing adapter boundary.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>crm-adapter persists tenant-aware delivery records and configuration through automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add board mapping configuration APIs</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests, shared/contracts</files>
  <action>Add focused mapping read/write routes and supporting helpers so current internal lead fields can be mapped to the selected Monday board without building a generic CRM mapper.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>Board mapping can be persisted and read back through adapter APIs covered by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/crm-adapter && npm test`
- [ ] Tenant-aware persisted state covers scans, leads, OAuth, and mapping data
- [ ] Board mapping APIs exist and are covered by tests
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has a persisted delivery/configuration foundation aligned to the existing crm-adapter
  </success_criteria>

<output>
After completion, create `.planning/phases/03-monday-delivery-flow/03-01-SUMMARY.md`
</output>

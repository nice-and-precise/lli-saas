---
phase: 03-monday-delivery-flow
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - services/crm-adapter/src
  - services/crm-adapter/tests
  - services/crm-adapter/README.md
autonomous: true
requirements:
  - EXP-01
  - EXP-03
user_setup: []
must_haves:
  truths:
    - crm-adapter can deliver leads with duplicate-aware behavior against the configured Monday board.
    - Delivery attempts and outcomes are persisted as operator-visible status records.
  artifacts:
    - services/crm-adapter/src/app.js
    - services/crm-adapter/src/mondayClient.js
    - services/crm-adapter/src/queries.js
    - services/crm-adapter/tests
  key_links:
    - Delivery flow uses the board mapping and tenant-aware state from 03-01 rather than hard-coded item behavior.
---

<objective>
Implement duplicate-aware Monday delivery and persisted delivery status.

Purpose: Turn the current one-item creation path into a real delivery workflow that checks for duplicates, records outcomes, and exposes durable operator-facing status information.
Output: Duplicate-aware delivery logic, delivery history/status persistence, and tests around created, skipped, and failed delivery cases.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monday-delivery-flow/03-CONTEXT.md
@.planning/phases/03-monday-delivery-flow/03-RESEARCH.md
@.planning/phases/03-monday-delivery-flow/03-VALIDATION.md
@.planning/phases/03-monday-delivery-flow/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add duplicate-aware delivery path</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests</files>
  <action>Add a stable duplicate key strategy and Monday query path so the adapter can detect existing items before creating a new lead item.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>crm-adapter can distinguish create-vs-skip outcomes through automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Persist delivery status and expose it cleanly</name>
  <files>services/crm-adapter/src, services/crm-adapter/tests, services/crm-adapter/README.md</files>
  <action>Persist delivery attempts and expose status/read APIs so operators and the portal can see created, skipped, duplicate, and failure outcomes tied to scan runs and leads.</action>
  <verify>cd services/crm-adapter && npm test && docker build -t lli-saas/crm-adapter:phase3 .</verify>
  <done>Delivery outcomes are durably recorded and documented, with adapter tests and Docker packaging still passing.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/crm-adapter && npm test`
- [ ] `cd services/crm-adapter && docker build -t lli-saas/crm-adapter:phase3 .`
- [ ] Delivery status records cover created, duplicate/skipped, and failed paths
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Monday delivery flow is duplicate-aware and produces persistent operator-visible status records
  </success_criteria>

<output>
After completion, create `.planning/phases/03-monday-delivery-flow/03-02-SUMMARY.md`
</output>

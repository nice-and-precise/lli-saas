---
phase: 03-monday-delivery-flow
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - services/user-portal/src
  - services/user-portal/tests
  - services/user-portal/README.md
  - services/crm-adapter/src
autonomous: true
requirements:
  - EXP-01
  - EXP-03
user_setup: []
must_haves:
  truths:
    - Operators can configure board mapping, trigger the first scan flow, and see delivery status from the portal.
    - Portal behavior is backed by real service state rather than Phase 1 placeholder copy.
  artifacts:
    - services/user-portal/src/App.jsx
    - services/user-portal/src/pages
    - services/user-portal/tests
    - services/crm-adapter/src
  key_links:
    - Portal/operator workflows surface the persisted mapping and delivery status produced by 03-01 and 03-02.
---

<objective>
Add first-scan orchestration and operator visibility to the portal.

Purpose: Replace the current static pilot shell with a real operator flow that can show mapping configuration, trigger the first scan-and-deliver path, and expose delivery status from backend state.
Output: Portal updates, supporting backend orchestration/status endpoints where needed, and tests/build verification for the operator flow.
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monday-delivery-flow/03-CONTEXT.md
@.planning/phases/03-monday-delivery-flow/03-RESEARCH.md
@.planning/phases/03-monday-delivery-flow/03-VALIDATION.md
@.planning/phases/03-monday-delivery-flow/03-01-SUMMARY.md
@.planning/phases/03-monday-delivery-flow/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend support for first-scan orchestration and status reads</name>
  <files>services/crm-adapter/src</files>
  <action>Add thin coordinating/status endpoints as needed so the portal can read persisted mapping and delivery state and trigger the first scan-to-delivery workflow without re-implementing backend logic in the UI.</action>
  <verify>cd services/crm-adapter && npm test</verify>
  <done>The portal has backend endpoints it can call for first-scan and status workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the static portal shell with an operator flow</name>
  <files>services/user-portal/src, services/user-portal/tests, services/user-portal/README.md</files>
  <action>Update the portal to show board mapping state, first-scan controls, and delivery status derived from real backend responses, then verify the flow through frontend tests and production build output.</action>
  <verify>cd services/user-portal && npm test && npm run build</verify>
  <done>The portal is no longer a static placeholder and exposes the pilot operator flow with automated coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd services/crm-adapter && npm test`
- [ ] `cd services/user-portal && npm test`
- [ ] `cd services/user-portal && npm run build`
- [ ] Portal shows mapping/first-scan/status flows backed by real service state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators can configure and observe the Monday delivery workflow from the portal
  </success_criteria>

<output>
After completion, create `.planning/phases/03-monday-delivery-flow/03-03-SUMMARY.md`
</output>

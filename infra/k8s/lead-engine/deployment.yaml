apiVersion: apps/v1
kind: Deployment
metadata:
  name: lead-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lead-engine
  template:
    metadata:
      labels:
        app: lead-engine
    spec:
      containers:
        - name: lead-engine
          image: ghcr.io/nice-and-precise/lli-saas-lead-engine:latest
          ports:
            - containerPort: 8000
          env:
            - name: PORT
              value: "8000"
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000

